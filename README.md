# Mock Pets API (static JSON)
Mock API for the Adopt a pet API

Endpoints:
- GET /api/pets            -> /api/pets.json
- GET /api/pets/:id        -> /api/pets/:id.json  (files are under /api/pets/{id}.json)
